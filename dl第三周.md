# 深度学习第三周-Transformer  
## 1.1Transformer整体架构  
- 位置编码 -> 词嵌入向量  
- 多头注意力机制  
- 层归一化和残杀连接  
- 前馈神经网络层  
- 多头注意力，带有因果掩码  
- 多头注意力  
- 全连接层  
- softmax，最终预测概率最高的词  
## 1.2输入文本数据数值转换  
- 独热编码向量维度大，浪费空间多，且不好呈现相关性  
## 1.3词嵌入向量  
- V为词表的数量  
- d为词嵌入矩阵大小，在Transformer里为512  
- 词嵌入矩阵式经训练得到的权重表，将词映射为向量  
## 1.4位置编码  
- 位置编码+词嵌入向量，得到语义+位置的信息  
- 其中PE(pos,2i) = sin(pos/10000**(2i/d))，PE(pos,2i+1) = cos(pos/10000**(2i/d))  
## 1.5自注意力机制的算法流程  
- 词与权重矩阵相乘，再与其他结果再相乘，用softmax映射为相关程度  