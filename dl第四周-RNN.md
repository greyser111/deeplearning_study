# 深度学习第四周-RNN  
## 1.基本组成结构  
### 1.1RNN基本结构  
- 为了解决存在先后关系的问题，采取记忆系统，记忆包含之前的输入信息。具体为将隐藏层的数据存储，和下一次的输入一起作为参数输入，并根据隐藏层数据再更新存储的数据。  
- 还包括深度RNN，双向RNN  
- BPTT算法，h=tanh(Ux + Wh)，y'=softmax(Vh)。则t时刻损失E=-y*log(y')。损失函数求导要一直延续，从t记忆一直求导到初始记忆，且tanh导数依然在0-1之间，仍存在梯度消失问题，太不优雅。  
## 2.变种  
### 2.1传统RNN问题  
- 长时依赖问题，比如答案提示离填空过远，会导致之前的信息在之后信息的补充下丢失的问题。(Transformer呼之欲出，看到这里才理解为什么会有注意力机制，可以有效解决当模型输出时应该注意哪些有用的信息！)  
### 2.2LSTM  
- 遗忘门，决定丢弃什么信息。  
- 输入门，确定需要更新的信息。  
- 输出门，确定哪个部分输出出去。  
### 2.3LSTM变种  
- peephole连接  
- 遗忘门和输入门连接  
## 3.扩展  
- 根据当前记忆生成Attention权重矩阵，告诉当前需要关注哪方面。  
- RNN做了一个很强的**先验假设**，逻辑相同，如名词-动词-名词，所以局限性也很大。总之，RNN的循环，用输出再当做输入和记忆系统有很大启发。  